<UserControl x:Class="LoneEftDmaRadar.UI.Radar.Views.HideoutTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LoneEftDmaRadar.UI.Radar.ViewModels"
             mc:Ignorable="d"
             Padding="10">

    <d:UserControl.DataContext>
        <d:DesignInstance Type="{x:Type vm:HideoutViewModel}"
                          IsDesignTimeCreatable="False" />
    </d:UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical"
                    Margin="0,0,0,10">

            <!-- General card -->
            <GroupBox Header="General" Margin="0,0,0,8">
                <StackPanel>
                    <CheckBox Content="Enable Hideout Tracking"
                              ToolTip="Highlights loot items needed for selected hideout upgrades."
                              IsChecked="{Binding Enabled, Mode=TwoWay}"
                              FontWeight="SemiBold"
                              Margin="0,0,0,4" />
                    <CheckBox Content="Show on Map/ESP"
                              ToolTip="Display hideout items on radar and ESP."
                              IsChecked="{Binding ShowHideoutItems, Mode=TwoWay}"
                              IsEnabled="{Binding Enabled}" />
                </StackPanel>
            </GroupBox>

            <!-- Station selection card -->
            <GroupBox Header="Stations"
                      Margin="0,0,0,8"
                      IsEnabled="{Binding Enabled}">
                <StackPanel>
                    <TextBlock Text="Select hideout stations and levels to track:"
                               Margin="0,0,0,8"
                               Foreground="{DynamicResource App.TextMutedBrush}"
                               FontSize="12" />

                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  MaxHeight="350">
                        <ItemsControl ItemsSource="{Binding Stations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource App.BorderBrush}"
                                            BorderThickness="0,0,0,1"
                                            Padding="0,4"
                                            Margin="0,0,0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="140" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0" />

                                            <ItemsControl Grid.Column="1"
                                                          ItemsSource="{Binding Levels}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <ToggleButton IsChecked="{Binding IsTracked, Mode=TwoWay}"
                                                                      MinWidth="32"
                                                                      Height="24"
                                                                      Margin="0,0,4,0"
                                                                      Padding="6,2"
                                                                      ToolTip="{Binding ItemSummary}">
                                                            <ToggleButton.Style>
                                                                <Style TargetType="ToggleButton">
                                                                    <Setter Property="Background" Value="#0C1A1A28" />
                                                                    <Setter Property="Foreground" Value="{DynamicResource App.TextSecondaryBrush}" />
                                                                    <Setter Property="BorderBrush" Value="{DynamicResource App.BorderBrush}" />
                                                                    <Setter Property="BorderThickness" Value="1" />
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="ToggleButton">
                                                                                <Border x:Name="Bd"
                                                                                        Background="{TemplateBinding Background}"
                                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                                        CornerRadius="3"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                                      VerticalAlignment="Center" />
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsChecked" Value="True">
                                                                            <Setter Property="Background" Value="#1A3B82F6" />
                                                                            <Setter Property="BorderBrush" Value="{DynamicResource Neon.CyanBrush}" />
                                                                            <Setter Property="Foreground" Value="{DynamicResource Neon.CyanBrush}" />
                                                                        </Trigger>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="BorderBrush" Value="{DynamicResource Neon.PurpleSoftBrush}" />
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </ToggleButton.Style>
                                                            <TextBlock Text="{Binding Level}"
                                                                       FontWeight="Bold"
                                                                       FontSize="11" />
                                                        </ToggleButton>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Currently Tracking"
                      Margin="0,0,0,8"
                      IsEnabled="{Binding Enabled}">
                <StackPanel>
                    <TextBlock Margin="0,0,0,8">
                        <Run Text="Items: " />
                        <Run Text="{Binding TrackedItemCount, Mode=OneWay}"
                             FontWeight="Bold"
                             Foreground="Orange" />
                    </TextBlock>
                    <ListBox ItemsSource="{Binding TrackedItems}"
                             Height="150"
                             ScrollViewer.CanContentScroll="True"
                             Background="Transparent">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel IsVirtualizing="True"
                                                        VirtualizationMode="Recycling" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsFound, Mode=TwoWay}"
                                              ToolTip="Mark as found (excludes from radar/ESP)"
                                              VerticalAlignment="Center"
                                              Margin="0,0,6,0" />
                                    <TextBlock VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFound}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough" />
                                                        <Setter Property="Opacity" Value="0.5" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                        <Run Text="{Binding ItemName, Mode=OneWay}"
                                             FontWeight="Bold" />
                                        <Run Text=" x" />
                                        <Run Text="{Binding CountRequired, Mode=OneWay}" />
                                        <Run Text=" for " />
                                        <Run Text="{Binding StationName, Mode=OneWay}"
                                             Foreground="Orange" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
